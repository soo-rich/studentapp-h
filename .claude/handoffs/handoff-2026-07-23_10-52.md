# Handoff — 2026-07-23 10:52 UTC

## Objectif de la session

Reprendre le handoff précédent (2026-07-21) et **terminer l'Épic 1 (Authentification &
vérification des profils) côté front `studentapp`** : trancher la tâche T2 (upload documents)
laissée non validée, livrer les écrans de modération (T4), et câbler les routes + page
d'accueil (T5). Orchestration stricte via sous-agents (explorer/implementer/validator), gate
de validation actif.

## Tentatives échouées

| # | Approche | Cause de l'échec |
|---|----------|-----------------|
| 1 | T2 (upload documents) validé a posteriori tel quel | **FAIL validator** : 5 tests de `verification-documents.spec.ts` rouges de façon déterministe — assertions synchrones sur une query TanStack async non résolue (composant OK, build vert, tout le reste PASS). Corrigé en T2-fix (`vi.waitFor`) → re-validé PASS. |
| 2 | T4 (écrans modération) validé sur première livraison | **FAIL validator** : 1 test **tautologique** dans `detail.spec.ts` — `expect(reject).not.toHaveBeenCalled()` s'exécutait avant le dispatch async de `mutate()`, donc vert même garde-fou retiré (prouvé par mutation). Composant `detail.ts` correct. Corrigé en T4-fix (flush macrotask `await new Promise(r => setTimeout(r))` avant l'assertion négative) → re-validé PASS, mutation tue désormais le test. |

## Tentatives réussies

| # | Approche | Pourquoi ça a marché |
|---|----------|---------------------|
| 1 | Cadrage orchestrateur : lecture directe du contrat (OpenAPI v0.2.0, paths upload 156-276 + modération 277-511) + couche data existante avant de briefer | Briefs T4/T5 sans ambiguïté (paths, DTO, noms de classe exacts, route shape imposée). |
| 2 | Lancement T2-validation + T4-implementation **en parallèle** (périmètres disjoints `verification/ui` vs `moderation/`) | Parallélisme réel sans conflit, gate respecté. |
| 3 | T2-fix scopé au **seul** `verification-documents.spec.ts` (`vi.waitFor`) | VALIDÉ PASS : 16/16 verts, assertions d'origine préservées, code applicatif intact. |
| 4 | T4 (écrans modération, 6 fichiers) : queue paginée + détail (download objectURL révoqué, approve/reject motif 3-500), consommation stricte de `moderation.queries.ts` | VALIDÉ PASS après T4-fix. Commit `83cde18`. Suite 109/109. |
| 5 | T4-fix : anti-tautologie par flush macrotask (piège `vi.waitFor(not.toHaveBeenCalled)` explicitement évité — passe au 1er tick) | Re-validé PASS : mutation (retrait garde-fou) rend le test ROUGE `been called 1 times [user-1, ab]`, restauré → vert. |
| 6 | T5 (intégrateur routes) : câblage login/register/verification/moderation + activation home + suppression stub `ModerationDashboard` | VALIDÉ PASS 7/7. Commit `6a4a486`. Build vert, 109/109. |
| 7 | Commits scopés par périmètre après validation + baselines git (levée du risque « untracked » signalé par le validator) | 6 commits propres : `83cde18` (T4), `e8d01c5` (T2-fix), `c0e71df` (doc), `6a4a486` (T5), `82a8be4` (roadmap). |

## Prochaines étapes

- [ ] **QA visuelle mobile-first** (~375–430px) des écrans Épic 1 réels : home (boutons actifs), login, register étudiant/recruteur, `etudiant|recruteur/verification` (upload), `moderation` (queue + `:userId` détail, download/approve/reject). Non fait cette session (build + 109 tests verts, mais pas de vérif navigateur) — candidat `playwright` ou dev server `npm start`.
- [ ] **Attaquer l'Épic 2 — Profil étudiant** : formulaire de profil (identité, université + n° carte, filière/niveau, expériences/compétences, langues, horaires, résidence, type d'opportunité) + **champs sensibles avec consentement** (logement, aptitudes/handicap, allergies — visibilité modération uniquement, jamais recruteur) + option « urgence ». Contrat d'API à définir (orchestrateur) AVANT délégation : l'OpenAPI `studentapi` v0.2.0 ne couvre pas encore l'Épic 2.
- [ ] **Dette technique — module `core/http` partagé** : `extractErrorMessage`/`isApiErrorBody`/`ApiErrorBody` désormais **dupliqués 4×** (login, register-etudiant, register-recruteur, moderation/detail) ; `ErrorResponse` redéfini dans `verification.types.ts` et `moderation.types.ts`. Décision orchestrateur : mutualiser (une seule tâche dédiée, pas un refacto au fil de l'eau).
- [ ] **Bundle budget** : warning de build persistant `522.43 kB > 500 kB` (initial bundle). Non bloquant mais à surveiller — envisager relever le budget dans `angular.json` ou lazy-splitter davantage.
- [ ] **Housekeeping back** (hérité, non fait) : `studentapi/CLAUDE.md` périmé (« squelette Nest, aucun module métier » alors que l'Épic 1 est complet) + commentaires obsolètes « NOT imported by AppModule yet » dans `verification.module.ts`/`moderation.module.ts` ; vérifier `typeorm` en `^1.1.0` dans `studentapi/package.json` (suspect vs `@nestjs/typeorm@^11`).

## Notes additionnelles

- **Épic 1 front = TERMINÉ, routé, atteignable end-to-end.** T1→T5 tous VALIDÉS + login partagé / register-étudiant (sessions précédentes). Roadmap Épic 1 cochée (`82a8be4`).
- **Contrat = OpenAPI v0.2.0**, figé et complet pour l'Épic 1. `studentapp/CLAUDE.md` corrigé (v0.1.0 → v0.2.0, périmètre Épic 1 ajouté, commit `c0e71df`).
- **Route shape (décision orchestrateur, à respecter par l'Épic 2)** : `moderation` index = `ModerationQueue`, `moderation/:userId` = `ModerationDetail` ; `verification` sous `etudiant` ET `recruteur` (même composant `VerificationDocuments`, types contraints par le rôle courant via `SessionService`). Router **sans** `withComponentInputBinding()` → params de route lus via `ActivatedRoute` + `toSignal`.
- **Exception documentée** au « tout passe par TanStack Query » : `ModerationApiService.downloadDocument` (flux binaire ponctuel) appelé directement dans `detail.ts` — volontaire, pas un composable.
- **Leçon de test (à réappliquer)** : pour une assertion négative sur un mock appelé via `mutate()` TanStack (asynchrone), NE PAS utiliser `vi.waitFor(() => expect(x).not.toHaveBeenCalled())` (passe au 1er tick, tautologique). Flusher la macrotask (`await new Promise(r => setTimeout(r))`) puis asserter. Tout test de blocage doit être prouvé par mutation (casser le garde-fou → test rouge).
- **État git en fin de session** : `master` à `82a8be4`, arbre **propre**. 6 commits ajoutés cette session au-dessus de `9ebc419`.
- **Note process** : le gate Stop hook `require-validation.sh` boucle tant qu'un implementer n'est pas passé au validator ; les validations complètes (build + suite 109 + mutation/revert) prennent 8–14 min — prévoir des waiters d'arrière-plan pour ne pas boucler à vide sur le gate.
