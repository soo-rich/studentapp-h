# Handoff — 2026-07-24 18:18 UTC

## Objectif de la session

Reprendre au point du handoff précédent (contrat Épic 3 v0.4.0 gelé) et **implémenter l'Épic 3
de bout en bout** — offres recruteur, candidatures étudiant, filtrage des candidatures par la
modération — en contract-first : d'abord la **vague back** (`studentapi`, NestJS) sur le contrat
gelé, puis la **vague front** (`studentapp`, Angular). Orchestration par sous-agents
(`explorer` → `implementer` → `validator`), validation sur diff réel à chaque tâche.

## Tentatives échouées

| # | Approche | Cause de l'échec |
|---|----------|-----------------|
| 1 | Valider les features FRONT en parallèle, chacune scopée pendant que les autres agents écrivent encore | **Échec environnemental, pas de code.** `ng test` compile TOUS les `*.spec.ts` dans un seul programme TS partagé (`tsconfig.spec.json`) : les fichiers non committés à moitié écrits des tâches sœurs cassent tout run scopé. Le validator F-MOD a rendu FAIL sur le seul critère « tests verts » alors que critères 1-7 = PASS. Corrigé : stratégie changée en **build + suite fork-unique sérialisés après commit** (voir mémoire `angular-shared-spec-program`). |
| 2 | Lancer la suite de tests front complète (pool `forks` Vitest par défaut) | `Error: spawn UNKNOWN` (errno -4094) / `Timeout waiting for worker to respond`, **0 test exécuté**. Le pool `forks` de Vitest sur cette machine Windows n'arrive pas à forker beaucoup de workers d'un coup (aggravé par la contention de 3 process Angular concurrents). Corrigé : `VITEST_MAX_FORKS=1 VITEST_MIN_FORKS=1` → suite complète verte (mémoire `vitest-fork-pool-windows`). |
| 3 | Attendre que les implementers front F-REC/F-ETU rendent leur rapport | Bloqués >30 min sur leur propre `ng test` final qui thrashait (même mur Vitest + contention). Fichiers pourtant tous écrits. Contournement : `TaskStop` des agents figés + du validator redondant, nettoyage des `ng serve` périmés, puis gate sérialisé par l'orchestrateur. |
| 4 | `TaskOutput`/attente naïve pendant que le gate `require-validation` boucle | Le hook `Stop` re-bloque le tour tant qu'un marqueur d'implementer non validé subsiste ; cycles à vide. Contournement fiable : attente bloquante sur un poll bash du dossier de marqueurs (`~/.claude/.state/pending-validation/<session>/*.pending`) via `TaskOutput block=true`. |

## Tentatives réussies

| # | Approche | Pourquoi ça a marché |
|---|----------|---------------------|
| 1 | **Back : B1 fondation bloquante → B2a/b/c en parallèle (périmètres disjoints) → B3 wiring** | Découpage par module NestJS. B1 (entités/enums/exceptions/i18n/migration réelle, `schema:log` propre) livré et validé avant tout le reste. B2a (recruiter-profile) / B2b (offers) / B2c (applications) sur dossiers disjoints → zéro conflit ; Jest s'isole par graphe de module donc validation scopée-en-parallèle OK côté back. |
| 2 | **Anti-fuite prouvée par mutation** (back `CandidateCard`) | Le validator a fait SA propre mutation (injecté `phoneNumber` dans `toCandidateCardDto`) → 1 test rouge (`Received: "90112233"`) → revert octet-pour-octet vérifié → 84/84 verts. Fixture remplissant les 10 champs interdits avec de vraies valeurs → test non tautologique. |
| 3 | **Front : F0 infra (moi) → F-REC/F-ETU/F-MOD en parallèle → F3 wiring routes (moi) → gate sérialisé** | F0 = ajout des préfixes `/recruiters` `/offers` à `proxy.conf.js` + `auth.interceptor.ts` (sinon ni proxy ni Bearer token). Routes et `app.module.ts`/`app.routes.ts` = domaine exclusif orchestrateur (chaque agent exporte un `*.routes.ts`, je câble). |
| 4 | **Gate autoritaire sérialisé front** | `npm run build` → exit 0 (bundle **530 ko raw / 146 ko gzip**, sous budget 600 ko, tout en lazy chunks) = preuve que tout compile + DI + routes. Puis suite complète fork-unique : **356/357** (l'unique échec = `student-profile-form.spec.ts` Épic 2, **flaky en fork-unique séquentiel, vert en isolation** — pas une régression). Validator final combiné sur l'arbre committé : **159/159** tests Épic 3 verts + anti-fuite front confirmée. |
| 5 | **Décisions de contrat tranchées par l'orchestrateur** (zones d'ombre remontées par l'explorer) | `residenceArea` = passthrough de `residenceLocation` (déjà au grain quartier/ville ; contrat gelé → pas de nouveau champ). `rejected_by_recruiter` = état terminal RÉSERVÉ sans transition productrice (pas d'endpoint recruteur de rejet en Épic 3). Contact recruteur = **404 cloaké** si candidature invisible, **409** si visible mais pas `accepted` (un 409 révélerait l'existence). |

## Prochaines étapes

- [ ] **Nav modération** : `layouts/moderation-layout` n'a aucune navigation interne — la file `/moderation/candidatures` (et `/moderation/urgences`) n'est atteignable que par URL. Gap pré-existant (jamais eu de nav modération) ; ajouter des liens vers les sections modération.
- [ ] **Dette DRY back** : `toOfferSummaryDto`/`OfferSummaryResponseDto` dupliqué dans `applications/dto/student-application-response.dto.ts` alors que `offers/dto/offer-summary.dto.ts` l'exporte (commentaire erroné « offers ne l'exporte pas »). Remplacer par un import direct depuis `offers`.
- [ ] **Dette préexistante** : `studentapi/src/users/users.service.spec.ts` (l.85, 103) — `TS2322`/`TS2783 'id' specified more than once`. Non bloquant (`nest build` exclut les specs, `jest` tolère), mais `tsc --noEmit` global reste rouge. Épic 0, hors Épic 3.
- [ ] **QA visuelle réelle** de l'Épic 3 (Playwright MCP à 375px) : parcours recruteur (créer/publier une offre → voir candidats anonymes → dévoiler contact au statut accepted), parcours étudiant (postuler → accepter/refuser), file modération (approuver/rejeter). Env dev à relancer (voir Notes).
- [ ] **Backlog hérité non soldé** : A5 (`studentapi/CLAUDE.md` périmé), A7 (retry TanStack 4× sur 404 → désactiver retry sur 4xx dans `QueryClient`), A8 (check post-build du poids gzip du bundle).
- [ ] **Épics ultérieurs** (contrat à étendre par l'orchestrateur avant délégation) : matching offres/profils (classement de la file de candidatures), notifications, évaluation bidirectionnelle, signalement + suspension.

## Notes additionnelles

- **État git — arbres propres, tout committé.**
  - `studentapi` : `afa7b96` (contrat v0.4.0) → `50d846e` (B1) → `2135cac` (B2a) → `0180729` (B2b) → `1ccd05e` (B2c) → `5cacbf3` (B3 wiring). Build + **294 tests** verts.
  - `studentapp` : `11194ea` (F0 prefixes) → `bd3eb5d` (recruteur) → `32c8e30` (etudiant) → `0591573` (moderation) → `9a0b667` (wiring routes). Build + **159 tests Épic 3** verts.
- **Contrat = `studentapi/docs/openapi.yaml` v0.4.0, jamais modifié par un agent.** Toute évolution reste du ressort de l'orchestrateur.
- **Mémoires écrites cette session** (`~/.claude/projects/.../memory/`) : `vitest-fork-pool-windows` (forcer `VITEST_MAX_FORKS=1` pour les runs front multi-fichiers) et `angular-shared-spec-program` (finaliser une vague front par build + suite fork-unique sérialisés, pas en validation parallèle scopée).
- **Environnement de dev** (reproductible) : Postgres `cd studentapi && docker compose up -d postgres` (port **54329**, ne PAS lancer le compose entier — conflit MinIO) ; API `PORT=3001 pnpm run start:dev` ; front `API_TARGET=http://localhost:3001 npx ng serve --port 4201`. ⚠️ Nettoyer les `ng serve` périmés en fin de session (`Get-CimInstance Win32_Process -Filter "name='node.exe'"` → `Stop-Process`) — 4 serveurs orphelins accumulés saturaient le pool Vitest cette session.
- **Correctif INDEX.md** : le handoff `handoff-2026-07-24_10-45.md` de la session précédente n'avait jamais été committé (fichier untracked) ni indexé ; réintégré et committé cette session.
- **Process de validation** : chaque `implementer` back est passé par un `validator` indépendant sur diff réel (jamais sur parole). Front : validation par tâche impossible (voir échec #1) → une **validation finale combinée** sur l'arbre committé stable a remplacé les 3 validations par tâche.
