# Handoff — 2026-07-23 16:22 UTC

## Objectif de la session

Deux temps. (1) **Finir l'Épic 1 front** (auth & vérification) : trancher T2, livrer les
écrans de modération, câbler routes+accueil. (2) **Démarrer l'Épic 2 (Profil étudiant +
urgence)** : concevoir le contrat d'API (responsabilité orchestrateur), le faire valider,
puis lancer l'implémentation contract-first (back `studentapi` + front `studentapp`).

## Tentatives échouées

| # | Approche | Cause de l'échec |
|---|----------|-----------------|
| 1 | T2 (upload docs) validé tel quel | FAIL : 5 tests spec async synchrones → corrigé (`vi.waitFor`), re-PASS. |
| 2 | T4 modération 1re livraison | FAIL : test reject **tautologique** (assertion avant dispatch async de `mutate`) → corrigé (flush macrotask), re-PASS, mutation tue désormais le test. |
| 3 | Validateur **combiné** pour le trio back Épic 2 (T1/T2/T3) | Piège de process, pas de code : le gate `require-validation.sh` décrémente **1 marqueur par validateur** (pas par tâche). Un validateur combiné laisse des marqueurs ouverts → il faut ~1 validateur par implementer pour clore le tour. |

## Tentatives réussies

| # | Approche | Pourquoi ça a marché |
|---|----------|---------------------|
| 1 | Épic 1 front terminé bout-en-bout (T2 fix, écrans modération T4, routes+home T5) | Tous VALIDÉS sur diff réel (mutation à l'appui). Commits `e8d01c5`, `83cde18`, `6a4a486` + doc/roadmap. Épic 1 front complet et routé. |
| 2 | Contrat Épic 2 : exploration back (explorer) + cahier des charges, PUIS 3 arbitrages soumis à l'utilisateur | Décisions structurantes claires : table dédiée `student_profiles`, whitelist DTO manuelle pour les champs sensibles (pas de `@Exclude`), OpenAPI manuel. |
| 3 | Contrat OpenAPI **v0.3.0** écrit par l'orchestrateur, validé (parse + refs), commité (`studentapi 98d5d26`) | Contract-first respecté. Choix user : périmètre complet (profil+urgence+conso modération), disponibilités en créneaux JSONB, `studyLevel` texte libre. |
| 4 | Plan 18 tâches (planner) : chemin critique back `T2→T4→T7→T8`, front contract-first (0 dépendance back) | Périmètres disjoints identifiés, fichiers partagés isolés. |
| 5 | **Vague 1** (T1,T2,T3 back + T10,T11 front) en parallèle, périmètres disjoints | Tous VALIDÉS PASS. Back commité (`142cfa6` entités/migrations/erreurs, `ccbf2b0` typeorm.config=T4). Front commité (`5dfe20a` couches data). |

## Prochaines étapes

Reprendre l'exécution du plan Épic 2 (le plan complet est dans l'historique de cette session ;
le contrat figé est `studentapi/docs/openapi.yaml` v0.3.0). **Vague 1 (T1-T4, T10, T11) =
FAITE, validée, commitée.** Restent :

- [ ] **T5 — ProfileModule back** (`GET/PUT /students/me/profile`) : DTO input (class-validator, `@ValidateNested` sur availabilitySlots, `HH:mm`), service avec règle de consentement (champ sensible sans `sensitiveDataConsent=true` → 422 `PROFILE_SENSITIVE_CONSENT_REQUIRED` ; consentement repassé à false → efface les sensibles), **DTO de sortie whitelisté à la main** (`toStudentProfileDto()`), controller `@Roles(ETUDIANT)`, tests. Dépend de T1/T3/T4 (faits). Ne PAS wirer AppModule (T9).
- [ ] **T6 — UrgentRequestModule back** (`GET/POST /students/me/urgent-request`) : `@Roles(ETUDIANT)`, POST bloque si une demande `pending` existe (409 `URGENT_REQUEST_ALREADY_PENDING`, testé), GET = dernière demande (404 si aucune), tests. Dépend T2/T3/T4.
- [ ] **T7 — modération urgences back** (`GET /moderation/urgent-requests` paginé + `POST .../{id}/review` prioritize|dismiss, 409 `URGENT_REQUEST_INVALID_STATE`) : **modifie** `moderation.controller.ts`/`moderation.service.ts` (miroir `listVerifications`). Dépend T2/T3/T4. **Séquentiel avec T8** (mêmes fichiers).
- [ ] **T8 — vue profil modération back** (`GET /moderation/students/{userId}/profile`, StudentProfile complet) : modifie `moderation.*`. Dépend **T7 validé**. Mapper `toStudentProfileDto()` dupliqué (à comparer champ à champ avec T5).
- [ ] **T9 — wiring AppModule** (`ProfileModule` + `UrgentRequestModule` dans `imports`) : trivial, orchestrateur peut le faire. Dépend T5/T6.
- [ ] **T13 — écran formulaire profil** (front, `features/profile/ui/`) : ReactiveForms, `FormArray` skills/langues/opportunités + éditeur de créneaux, section sensible **masquée tant que consentement non coché** (vide les valeurs si décoché avant submit), pré-remplissage via `injectStudentProfileQuery` (404 = formulaire vierge). Dépend T10 (fait).
- [ ] **T14 — écran urgence étudiant** (front, `features/urgent-request/ui/`) : si `pending` → statut affiché, form masqué ; sinon form (message 10-1000). Dépend T11 (fait). Ton neutre/rassurant (détresse).
- [ ] **T15/T16/T17 — écrans modération** (front) : file urgences paginée (`/moderation/urgences`), traitement (`/moderation/urgences/:id`, prioritize|dismiss + note), vue profil complet (`/moderation/etudiants/:userId`, champs sensibles marqués restreints). Dépendent de **T12**.
- [ ] **T12 — extension couche data modération front** (modifie `moderation/data/**`, additif) : `injectUrgentQueueQuery`, `injectReviewUrgentRequestMutation`, `injectModerationStudentProfileQuery`, réexport `StudentProfile`. Dépend T10/T11 (faits). **Prérequis de T15/16/17.**
- [ ] **T18 — câblage routes front** : `etudiant/profil`, `etudiant/urgence`, `moderation/urgences`, `moderation/urgences/:id`, `moderation/etudiants/:userId`. Dépend T13-T17. Trivial, orchestrateur.
- [ ] **QA visuelle mobile** (héritée Épic 1, non faite) + housekeeping back (`studentapi/CLAUDE.md` périmé).

## Notes additionnelles

- **Contrat = `studentapi/docs/openapi.yaml` v0.3.0**, figé, complet pour l'Épic 2. Ne le modifier que par l'orchestrateur. Codes d'erreur Épic 2 déjà catalogués (exceptions + i18n fr/en, commit `142cfa6`).
- **Décisions d'architecture back actées** (rapport explorer) : 1 entité = 1 table dédiée (jamais de colonnes sur `User`) ; `typeorm.config.ts` a un array `entities` **explicite** (ajout manuel obligatoire — fait pour StudentProfile/UrgentRequest en T4) ; **aucune sérialisation déclarative** (`@Exclude` absent) → la SEULE barrière contre l'exposition des champs sensibles est le **DTO de sortie whitelisté à la main dans chaque service** (`toXxxDto()`), à imposer à chaque implementer back. 2 modules séparés `ProfileModule`/`UrgentRequestModule`.
- **Dette back repérée (non bloquante, validateurs)** : `StudentProfile.availabilitySlots` jsonb `default: "'[]'"` → préférer `default: () => "'[]'"` (sinon diff parasite si `migration:generate` un jour) ; `StudentProfile.createdAt/updatedAt` en `TIMESTAMP` sans fuseau alors que le reste est `timestamptz` — à uniformiser ; nommage des contraintes hétérogène entre les 2 migrations.
- **Migrations non exécutées** (pas de Docker/Postgres dispo) : validées par revue statique + build. À faire tourner (`pnpm migration:run`) quand une DB est disponible.
- **TypeORM `1.1.0`** confirmé (pas une erreur de lockfile) — s'en tenir aux patterns du repo (enum arrays, jsonb OK).
- **Package managers** : back = **pnpm** (`pnpm run build`/`pnpm test`) ; front = **npm** (`npm run build`/`npm test`, établi Épic 1). Ne jamais lancer d'install en tâche (lockfiles). Piège front : `ng build`/`ng test` peut bloquer sur le prompt analytics Angular CLI au 1er run → `NG_CLI_ANALYTICS=false CI=true`.
- **Piège de process — gate `require-validation.sh`** : **1 validateur = 1 marqueur** décrémenté. NE PAS utiliser de validateur combiné multi-tâches (laisse des marqueurs). Faire **1 validateur par tâche d'implementer** pour pouvoir clore le tour. Concurrence build : éviter 2 `build` simultanés sur le même repo (race `dist/`) — sérialiser par repo, paralléliser cross-repo (back ∥ front).
- **Suivi des agents** : les fichiers de transcript restent 0 octet jusqu'à la fin → ne PAS s'y fier ; se fier aux **task-notifications** (signal fiable). `git status` des repos est un bon proxy de progression.
- **État git fin de session** :
  - `studentapp` (front) `master` : ... `6a4a486` (T5 Épic1) → `82a8be4` (roadmap) → `b30671d` (handoff Épic1) → `5dfe20a` (T10/T11 data Épic2). Arbre propre attendu après le smoke final.
  - `studentapi` (back) `master` : ... `22c1f3e` (BE6) → `98d5d26` (contrat v0.3.0) → `142cfa6` (T1-T3) → `ccbf2b0` (T4). Arbre propre.
- **Dette technique front héritée** : `extractErrorMessage`/`ErrorResponse` dupliqués (≥4×) → futur module `core/http` partagé (tâche dédiée, décision orchestrateur).
