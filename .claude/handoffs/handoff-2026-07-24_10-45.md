# Handoff — 2026-07-24 10:45 UTC

## Objectif de la session

Reprendre au point laissé par le handoff précédent (Épic 2 terminé). Décision de priorité
prise avec l'utilisateur : **solder d'abord la dette technique accumulée**, puis démarrer
l'**Épic 3** (offres recruteur + candidatures étudiant + filtrage modération) en
**contract-first** — le contrat v0.4.0 écrit et gelé AVANT toute délégation d'implémentation.
Résultat visé : dette soldée (factorisation `core/http`, test de refetch réel, uniformisation
des champs, bundle) et contrat v0.4.0 écrit, validé, commité.

## Tentatives échouées

| # | Approche | Cause de l'échec |
|---|----------|-----------------|
| 1 | A1 (factorisation `core/http`) — l'implementer qualifie de « mutant équivalent » le retrait de la garde `instanceof HttpErrorResponse` | **Faux.** Le validateur a démontré une entrée qui diverge : un objet `Error`-like non-HTTP portant `.error.message` fait fuiter ce message à l'écran sans la garde. 107 tests restaient verts sous la mutation. Même classe de trou que la fuite de token de la session précédente (`startsWith`→`includes`). **Corrigé par l'orchestrateur** : 2 tests d'ancrage ajoutés, preuve par mutation refaite (2 rouges ciblés → restauration au checksum → 18/18 verts) plutôt que relancer un cycle implementer complet. |
| 2 | Recommander à l'utilisateur un « budget sur la taille compressée » dans `angular.json` | Techniquement impossible : les budgets Angular mesurent le **raw size** uniquement, pas le compressé. Rectifié à temps : budget raw porté à 600 ko, suivi du poids compressé reporté en tâche dédiée (A8, check post-build). |
| 3 | Premier `git commit -m` avec here-string PowerShell (`@'…'@`) dans le tool Bash | Parse error de Git Bash (`pathspec 'de' did not match…`). Le `git add` avait réussi. Contourné en écrivant chaque message de commit dans un fichier `.txt` (scratchpad) et `git commit -F`. Piège récurrent : ne pas mélanger syntaxe PowerShell et Bash. |
| 4 | Hypothèse du handoff précédent : le dépassement de bundle serait une **régression Épic 2** (Material dans les layouts) | **Infirmée.** Analyse : toutes les routes sont en `loadComponent` (lazy), `app.ts` n'importe que `Component`+`RouterOutlet`, Material est entièrement dans les chunks lazy (0 occurrence `mat-mdc` dans l'initial). L'initial est le socle framework nu (Angular+Router+HttpClient+TanStack Query+Tailwind). Aucune régression à isoler. |

## Tentatives réussies

| # | Approche | Pourquoi ça a marché |
|---|----------|---------------------|
| 1 | Vagues de 2 implementers en parallèle sur **périmètres disjoints** (A1 `core/http`+features vs A2 specs modération ; puis A3 `features/profile` vs A6 `features/moderation`) | Découpage par dossier : aucun conflit d'écriture bloquant. Une seule contention transitoire de compilation vue par A2 (fichier d'A1 en cours d'écriture), résolue seule. |
| 2 | Briefs de validation **ciblés sur le point faible de chaque tâche** | A2 : « le `QueryClient` est-il RÉELLEMENT partagé entre le composant monté et la mutation injectée ? » → le validateur a cassé la clé d'invalidation et vu **exactement** les 2 tests T20 rougir. A1 : « le mutant `instanceof` est-il vraiment équivalent ? » → trou révélé. A3 : piège `[attr.id]` prouvé en remettant `[attr.id]` (sélecteurs cassés). |
| 3 | Décision de contrat de l'orchestrateur : **mapper unique partagé refusé pour la vue recruteur** | Le mapper `toStudentProfileDto` sert déjà 2 vues identiques (étudiant + modération). La fiche recruteur passe par un DTO/mapper **séparé** (`CandidateCard` / futur `toCandidateCardDto`), whitelist stricte, jamais de flag conditionnel — un flag mal câblé = fuite. |
| 4 | **QA visuelle réelle à 375px** via Playwright MCP (compte QA créé par l'API, login navigateur, mesures DOM) | A confirmé les 3 points que le validateur A3 ne pouvait pas trancher : `scrollWidth==clientWidth` (zéro débordement, `min-w-0` efficace), cibles 44×44px, bouton supprimer aligné en haut (`items-start`). Hauteur de ligne 76px mesurée = estimation validateur juste. |
| 5 | **Contrat v0.4.0 écrit à la main** puis validé par script Python/PyYAML | Parse OK, **0 `$ref` cassée**, aucun schéma orphelin, tous les paramètres de chemin déclarés, cohérence 204/content. 19 paths, ~19 schémas nouveaux, greffé sans rupture sur les conventions v0.1→v0.3 (pagination `{items,page,pageSize,total}`, erreurs inline, statuts anglais / taxonomies FR). |
| 6 | Suite complète et build **sérialisés par l'orchestrateur** en fin de vague | Pas de contention CPU (interdiction faite aux agents de lancer la suite complète ou un build). front **198/198** verts, build sans warning après recalibrage. |

## Prochaines étapes

- [ ] **Épic 3 — vague back (`studentapi`)** sur le contrat gelé `afa7b96`. Ordre suggéré :
      (1) entités TypeORM `RecruiterProfile`, `Offer`, `Application` + enums (`RecruiterStructureType`,
      `OfferStatus`, `ApplicationStatus`) + migrations, exécutées réellement puis `schema:log` ;
      (2) services + mappers **anti-fuite** — `toCandidateCardDto` en whitelist stricte (jamais nom,
      contact, n° carte, userId, ni aucun champ sensible ; preuve par mutation : retirer un champible
      sensible du mapper doit rougir un test) ; endpoint `.../contact` servi UNIQUEMENT si statut
      `accepted`. Contract-first : ne pas dévier du contrat, remonter à l'orchestrateur si besoin.
- [ ] **Épic 3 — vague front (`studentapp`)** ensuite : espaces recruteur (profil, offres, candidats
      anonymes) et étudiant (parcours d'offres, candidatures, accept/decline), file de candidatures
      côté modération. Réutiliser `core/http` (fraîchement livré) pour la gestion d'erreurs, et le
      pattern TanStack Query (`*.keys.ts` / `*.queries.ts`).
- [ ] **A5 (housekeeping)** : `studentapi/CLAUDE.md` toujours périmé (signalé sur 4 sessions).
- [ ] **A7** : TanStack Query retente **4 fois** un 404 nominal (`GET /students/me/profile` sur profil
      inexistant → 4 requêtes). Désactiver le retry sur les 4xx dans la config du `QueryClient`
      (`app.config.ts`). Impact réseaux mobiles togolais. Non bloquant.
- [ ] **A8** : ajouter un check post-build du **poids compressé** (~145 ko actuel) — les budgets
      `angular.json` ne mesurent que le raw. Script Node comparant le gzip des chunks initiaux à un
      seuil (~160 ko).
- [ ] **Uniformiser les champs texte `student-profile-form`** : point de suivi hérité (les `FormArray`
      sont maintenant en Material, cf A3 ; vérifier qu'aucun autre champ brut ne subsiste). Envisager
      `subscriptSizing="dynamic"` pour réduire la hauteur (+60%/ligne mesurée) sur profils longs.

## Notes additionnelles

- **Contrat = `studentapi/docs/openapi.yaml`, désormais v0.4.0** (commit `afa7b96`). Toute modification
  reste du ressort exclusif de l'orchestrateur ; les agents le CONSOMMENT.

- **Décisions produit/contrat actées cette session** (validées avec l'utilisateur) :
  1. **Vue recruteur** = fiche `CandidateCard` **anonyme** (filière, niveau, université, compétences,
     langues, expériences, disponibilités, `residenceArea` approximative). Identité + contact dévoilés
     UNIQUEMENT via `GET /recruiters/me/offers/{offerId}/candidates/{applicationId}/contact`, accessible
     au seul statut `accepted` — après sélection recruteur ET accord explicite de l'étudiant. Seul point
     du contrat exposant l'identité d'un étudiant à un recruteur.
  2. **Flux nominal** = candidature **initiée par l'étudiant** (`POST /offers/{offerId}/applications`).
     Le matching automatique (épic ultérieur) ne sera qu'un **classement** de cette file, pas un 2e canal.
  3. **Pas de modération préventive des offres** : le recruteur est déjà vérifié (Épic 1), il publie
     `draft → published` lui-même. La lutte contre les fausses offres relèvera du **signalement** (épic
     ultérieur). La modération ne filtre QUE les candidatures.
  4. **Machine à états de candidature** (`ApplicationStatus`) : `pending_moderation` → `forwarded` →
     `selected` → `accepted` | `declined`. Terminaux : `rejected_moderation`, `rejected_by_recruiter`,
     `declined`, `accepted`, `withdrawn`. Codes d'erreur génériques `APPLICATION_INVALID_STATE` /
     `OFFER_INVALID_STATE` pour les transitions interdites.
  5. **Budget bundle** recalibré à **600 ko raw** (le défaut 500 ko du scaffold n'était pas adapté à
     la stack). Le vrai chiffre à suivre est le **poids compressé** (~145 ko), non mesurable nativement
     par `angular.json` → A8.

- **`core/http` livré** (`studentapp/src/app/core/http/`) : `ApiErrorResponse` (5 champs du contrat),
  `PartialApiErrorBody`, `isApiErrorBody`, `extractErrorMessage`, `extractErrorCode<TCode>`. Les 3 types
  `data/*.types.ts` en dérivent (`Omit<ApiErrorResponse,'error'> & { error: XxxCode }`). Deux comportements
  volontairement préservés, NON harmonisés : `urgent-request-page` détecte son 404 sur le statut HTTP brut
  (pas le code machine) ; `queue.ts`/`urgent-queue.ts` importent la constante mais n'affichent JAMAIS le
  message backend (pas de `extractErrorMessage` chez eux).

- **Faits techniques vérifiés / gravés dans `studentapp/CLAUDE.md`** (`7f42411`) : scoper `ng test` se fait
  via `--include <glob> --watch=false` (répétable) ; la forme `ng test -- <args>` est **rejetée** par le
  builder Angular. Lancer la suite complète **uniquement sérialisée** (la contention CPU produit de faux
  `Test timed out in 5000ms` sur des fichiers sains).

- **Environnement de dev opérationnel** (identique au handoff précédent, reproductible) :
  - Postgres : `cd studentapi && docker compose up -d postgres` (port **54329**). Ne PAS lancer le compose
    entier (ports MinIO en conflit).
  - API : `PORT=3001 pnpm run start:dev` (3000 pris par une app Next.js d'un autre projet).
  - Front : `API_TARGET=http://localhost:3001 npx ng serve --port 4201` (4200 déjà occupé).
  - Ces trois services étaient debout en fin de session ; un compte QA `qa.visuel.a3@example.com` (mdp
    `MotDePasse123!`, rôle etudiant, profil non renseigné) a été créé pour la QA visuelle A3.

- **Process** : le gate `require-validation.sh` bloque la fin de tour tant qu'un `implementer` n'est pas
  passé par le `validator` (marqueurs dans `~/.claude/.state/pending-validation/<session>/`). D'où de
  longues attentes ; ne jamais lancer plus d'agents que nécessaire, jamais de validateur multi-tâches.

- **État git fin de session — arbres propres des deux côtés** :
  - `studentapp` : `8c26ee9` → `a697166` (A1) → `1d324e6` (A2) → `175f675` (A6) → `3f48cb4` (A3) →
    `7f42411` (doc ng test) → `ed620e5` (budget).
  - `studentapi` : `7d039be` → `afa7b96` (contrat v0.4.0).

- **Vérifications finales** : front **198/198** tests + build sans warning · contrat v0.4.0 parse-validé
  (0 `$ref` cassée, aucun orphelin, params de chemin OK).
